# Copy this file to .env.local and fill in your Clerk publishable key:
#   cp web/.env.example web/.env.local
#
# Get the key from https://dashboard.clerk.com (Frontend / Publishable key).
# Must use the VITE_ prefix so Vite exposes it to the browser.
VITE_CLERK_PUBLISHABLE_KEY=
